should we be size-aware when computing the flatten /filter


e.g. do we allocate the intermediate filtered arrays for subseqences during the filter?

write filter using library from previous paper 


implement the data type T as a POD (plain old datatype)

MapReduce first

then do filter (simple filter)

flatten can follow based on the block implementation



What to do now:

Block scan (finished)
MultiSSD filter
Clean up code
decide on a sequence representation
look into existing delayed sequence papers
